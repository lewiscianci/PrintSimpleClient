using System.Reflection;
using log4net;

namespace PrintSimple.Classes
{
    internal class CredentialOperations
    {
        private static readonly ILog log = LogManager.GetLogger
            (MethodBase.GetCurrentMethod().DeclaringType);
    }
}